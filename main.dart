void main() {
  var name = 'imonkfcwifi';
  var age = 23;
  var greeting = 'halo my name is $name ! and my age is ${age + 2}';

  print(greeting);
  // $뒤에 변수 사용
  // i'm 에 ' 가 들어있기 때문에 문제가 될 수 있으므로 I\'m으로 해줘야 함
}
